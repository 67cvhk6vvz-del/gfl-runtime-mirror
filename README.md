# GFL Runtime Mirror

Runtime bridge for the 2026 GFL operation.

Flow:

`ESPN -> existing Netlify gfl-live-transport -> GitHub Action -> GitHub Pages state.json -> ordinary ChatGPT / Theo`

The repository stores no ESPN credentials.
